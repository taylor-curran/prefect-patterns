# File for configuring project / deployment build, push and pull steps

# Generic metadata about this project
name: prefect-patterns
prefect-version: 2.10.13

# Where do i put in the requirments, it didn't work when I added them to line 12

# build section allows you to manage and build docker images
build:
- prefect_docker.projects.steps.build_docker_image:
    requires: prefect-docker
    image_name: taycurran/test-projects-june11
    tag: latest
    dockerfile: auto
    push: true

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.projects.steps.git_clone_project:
    repository: https://github.com/taylor-curran/prefect-patterns.git
    branch: main
    access_token: null
